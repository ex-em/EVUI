# Tree (EvTree) — Decisions

<!-- 날짜 `-` = SPEC 아키텍처 관찰 기반 초기 결정. -->

| 날짜 | 결정 | 이유 | 대안 |
|------|------|------|------|
| - | 노드 상태를 `allNodeInfo` 배열로 평탄화 관리 | parent 키로 상향 탐색(updateTreeUp)이 O(1), 재귀 트리를 매번 순회하지 않음 | 중첩 객체 직접 순회(상향 탐색 비용) |
| - | `src/common/utils.tree.js`(#2323 에서 제거) 대신 자체 노드 관리 | 체크 연동·검색·확장이 tree 전용 로직이라 공용 유틸로 커버 불가 | 공용 utils.tree 확장(도메인 결합) |
| - | 자식→부모 체크 전파(updateTreeUp) | 계층 체크의 직관적 동작(자식 다 체크 시 부모 체크) | 노드 독립 체크(계층 의미 상실) |
| - | TreeNode 재귀 컴포넌트로 계층 렌더 | 깊이 무제한 트리를 재귀로 자연스럽게 표현 | 평면 반복 + 들여쓰기 계산(복잡) |
