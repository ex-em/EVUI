# Chart (EvChart) — Decisions

<!-- 주요 설계 결정과 이유. 새 결정은 테이블 맨 아래에 추가. -->
<!-- 날짜 `-` = SPEC 아키텍처 관찰 기반 초기 결정(정확한 커밋 미추적). YYYY-MM-DD = git 이력으로 근거 확인된 결정. -->
<!-- 범위: 차트 루트 + plugins/element/scale/model 하위 전체. -->

| 날짜 | 결정 | 이유 | 대안 |
|------|------|------|------|
| - | Canvas 2D 기반 렌더링 채택 (line/bar/pie/scatter/heatMap) | 수천~수만 데이터 포인트를 요소당 DOM 노드 없이 그려 렌더/메모리 비용을 억제 | SVG(요소 수 폭증), DOM/HTML 차트 |
| - | 3장 canvas 더블 버퍼 구조 (buffer 래스터 → display blit present, overlay 즉답 레이어) | present 시점 atomic blit 으로 깜빡임 제거, hover/crosshair 는 overlay 로 시리즈 재래스터 없이 즉답 | 단일 canvas 직접 렌더(부분 갱신마다 전체 재그림·깜빡임) |
| - | 부가 계층(plugins/model/element.tip)을 클래스가 아닌 메서드 모음 객체로 `Object.assign(this, Module)` flat mixin | EvChart 인스턴스 상태(options/data/seriesList/ctx…)를 `this` 로 직접 공유하며 파일별로 관심사 분리, own-property 라 프로토타입 메서드보다 우선 | 상속 계층, 별도 인스턴스 필드(컴포지션)로 상태 전달 |
| - | element 시리즈 렌더러를 타입별 클래스 + duck-typed 인터페이스(draw/computeGeometry/findGraphData/findItems/itemHighlight)로 구성 | 타입별 렌더/히트 로직을 독립 캡슐화하고 코어는 공통 계약으로만 호출 | 코어 내부 타입 분기 스위치, 단일 거대 렌더 함수 |
| - | 축 스케일을 Scale 베이스 + 5개 구현(Linear/Logarithmic/Time/TimeCategory/Step) 인스턴스로 분리하고 `createAxes` 팩토리가 axis type/categoryMode 로 선택 | tick 계산·라벨·plot 렌더가 축 종류마다 크게 달라 다형 인스턴스로 격리, 축별 상태를 인스턴스에 보존 | 축 타입별 if/switch 분기 함수, 단일 스케일에 옵션 플래그 |
| - | brush 차트(`options.brush`)는 tooltip/legend/interaction/tip/pie/title/scrollbar mixin 과 overlay canvas 를 아예 미장착 | EvChartBrush 미니 차트는 렌더 전용이라 인터랙션·DOM 계층이 불필요 — 생성 비용·DOM 누수 원천 제거 | 전 mixin 장착 후 런타임 플래그로 비활성 |
| - | 데이터/옵션 정규화를 props 와 분리된 새 객체로 생성(`defaultsDeep`/`defaults` 로 누락 키만 병합) | 소비자 props 참조와 내부 상태를 분리해 오염 방지 | 원본 props 에 in-place 병합 |
| 2026-05-13 | scatter 좌표 dedupe 를 `coordinateDedupe` opt-out + O(1) Set 으로 정리 (기본 on) | cross-series 동일 좌표 overdraw 제거하되 #2011 "모든 중복 표시" 요구를 opt-out 으로 수용, 버킷별 Set 으로 push 시점 O(1) 판정 | 항상 dedupe(중복 표시 불가), 렌더 단계 O(n) 좌표 비교 |
| 2026-05-28 | 커스텀 HTML 툴팁(formatter.html)에 가상 스크롤 도입 (spacer/viewport 재구성, row 탐지) | 시리즈 수백 개 툴팁에서 전체 DOM 부착 비용·프레임 드랍 제거 | 전체 행 DOM 부착, 툴팁 행 수 상한 |
| 2026-06-01 | TimeCategoryScale 에 index window(minIndex/maxIndex, sentinel {0,-1}) 산출(`calculateScaleRange`) 추가 | axis range 로 일부만 보일 때 그릴 인덱스 범위를 스케일이 확정해 소비자 재계산·오설정을 차단 | 소비자마다 라벨 배열 재탐색, undefined=전체 관례(빈 윈도우와 모호) |
| 2026-06-04 | 스크롤바 range 에 `anchorEdge`(start/end/null) 도입 — 가장자리 부착 의도 보존 | 리사이즈/데이터 갱신 시 사용자가 붙여둔 가장자리를 유지, minMax 미확정(null) 시 range 불변으로 `+null` 오염 방지 | 매번 옵션 range 로 리셋(사용자 위치 상실) |
| 2026-06-10 | realtime scatter blit fast-path 도입 — 이전 점 라스터를 drawImage 로 좌측 시프트 + 신규 strip 만 재래스터 | 슬라이딩 윈도우 실시간 scatter 에서 틱당 전체 재래스터 비용 제거 | 매 틱 full redraw |
| 2026-07-20 | 어노테이션/뱃지 모듈(annotation/) 추가 — `options.annotations` 선언형 API + 순수 함수 파이프라인(normalize→resolve→layout→render) + 전용 `annotation-canvas` 지연 생성 레이어 | 차트 위 라벨/뱃지/말풍선/강조를 선언형으로 얹되, 순수 함수로 테스트 용이성을 확보하고 전용 레이어로 series 재래스터와 분리(격리·미사용 시 비용 0) | series 버퍼에 직접 그리기(재래스터 결합·격리 불가), DOM 오버레이 컴포넌트(좌표 동기화 비용) |
| 2026-07-20 | series 추적 어노테이션 `location` 의 `start`/`end` 를 데이터 있는(non-null) 첫/마지막 포인트로 정의 | 앞뒤 null 구간이 있는 시계열에서 빈 영역이 아니라 실제 데이터 경계에 어노테이션을 붙이기 위함 | 배열 물리 첫/마지막(0/length-1) 고정(null 구간에 표시됨) |
| 2026-06-11 | 데이터 파이프라인에서 cloneDeep/반응성 제거 (`toRaw` unwrap + normalize 비-변형) | 매 틱 깊은 복제·proxy trap 비용 제거, 원본 불변 유지 | 매 갱신 cloneDeep, reactive 값 직접 소비 |
| 2026-06-11 | 점 마커를 색상/그룹별 배치 렌더(`drawPointBatch`)로 전환, path-per-point 제거 | 다수 시리즈×다수 포인트에서 rasterizer flush 를 상수 회로 억제 | 포인트마다 beginPath/fill/stroke |
| 2026-06-11 | blit fast-path 를 `chart.blit.js` 프로토타입 모듈로 추출 | 코어에서 fast-path 를 격리해 유지보수성 확보, 테스트가 `Object.create(prototype)` 로 호출 가능 | chart.core.js 인라인 유지 |
| 2026-06-11 | 렌더 파이프라인을 RenderCore(DOM-free, ctx 주입) / ChartShell(main 전용 DOM·listener) 로 분리 | RenderCore 를 worker 재사용 가능하게 만들고 DOM 의존을 Shell 에 국한 | 단일 drawChart 에 DOM·렌더 혼재 |
| 2026-06-11 | worker(OffscreenCanvas) series 래스터 오프로드를 opt-in(`workerRender`, 기본 off)으로 추가 | 대량 시리즈 래스터를 메인 스레드에서 분리, 미지원·실패 시 main full redraw 무회귀 폴백 | 항상 main 렌더, worker 강제(호환성 리스크) |
| 2026-06-11 | props.data deep-watch opt-out(`shallowDataWatch`, 기본 off) | 초대형 데이터에서 deep watch 순회 비용 제거(소비자가 top-level 참조 교체 계약 수용 시) | 항상 deep watch |
| 2026-06-12 | options deep-watch opt-out(`shallowOptionsWatch`, 기본 off) | 큰 옵션 객체 deep watch 비용 제거 | 항상 deep watch |
| 2026-06-12 | line/bar geometry 메모이즈 — `(dataEpoch, scaleVersion[, showIndex, showSeriesCount])` + data 참조 키로 재계산 skip | hover/재렌더 프레임에서 불변 기하 재계산 제거(키는 숫자 비교로 문자열 할당 회피) | 매 프레임 computeGeometry 재실행 |
| 2026-06-15 | updateSeries 시 series 인스턴스 증분 재조정(`reconcileSeriesSet`) — reconcile key+opt 동일 시 재사용, index 판정 제외 | 실시간 series churn 프레임에서 점객체 풀·geometry 메모를 보존(full-recreate 대비 목표 5.5×), 색 팔레트가 이웃 churn 에 흔들리지 않게 | 매 갱신 seriesList 전체 재생성 |
| 2026-06-18 | selectSeries 선택 라인을 full redraw 직후 최상위로 한 번 더 덧그림(line-safe 게이트 한정) | dimmed 시리즈 위로 선택 라인이 묻히는 z-order 문제 해소 | 선택 라인 별도 처리 없음(묻힘), 전 시리즈 재정렬 |
| 2026-06-25 | 어노테이션/뱃지 모듈 추가 | 시리즈 위 주석·뱃지 표식을 별도 모듈로 제공 | 기존 plot/tip 계층에 혼재 |
| 2026-06-30 | plotLine/plotBand z-order 옵션(`plot.aboveSeries`) 추가 | plot 오버레이를 시리즈 위/아래 중 선택 가능하게 | 항상 시리즈 위(또는 아래) 고정 |
| 2026-06-30 | realTimeScatter 만료 series 즉시 자동 제거((a) 가시 윈도우 밖 + (b) 신규 점 없음, grace 없음) | pod 소멸 등으로 데이터가 끊긴 series 를 축 freeze 없이 정리, 부활 가드(`prunedRealTimeScatterSeries`)로 재추가 방지 | "series 키 부재"로 판정(실시간 배치와 불일치), grace 기간 유지 |
| 2026-06-30 | blit 경로를 full redraw 와 픽셀 동등(blit≡full)하게 확정 — 반투명(per-point `drawn` 플래그)·분수 DPR(q배수 시프트로 device px 정수화)·cross-series dedupe 복원 | fast-path 가 알파 누적·sub-pixel drift·overdraw 로 full 과 달라지는 회귀를 제거, 조건 미달 시 full 폴백 유지 | 불투명·정수 DPR 로 fast-path 적용 범위 제한 |
| 2026-07-24 | plot 라벨 `valueFormatter` 의 폴백을 null/undefined 반환에만 적용하고, 그 외 반환값은 `String()` 변환 | return 누락 등으로 null/undefined 가 오면 라벨에 리터럴 "null"/"undefined" 가 그려지므로 막아야 한다. 반면 숫자 반환은 사용자가 계산한 결과이므로 폴백하면 의도가 사라진다 — 축 `formatter`(문자열 아닌 모든 반환값을 폴백)와 규칙이 다른 이유 | 축 formatter 와 동일하게 문자열 외 전부 폴백(계산 결과 유실), 무조건 `String()`(리터럴 "null" 노출) |
| 2026-07-31 | realTimeScatter 만료 판정 (a) 의 좌단을 렌더 좌단(`fromTime + 1000` = `globalToTime - (range-1)*1000`) 에 맞춤 | 기존 `globalToTime - range*1000` 은 링이 보유·렌더하는 가장 오래된 버킷보다 1버킷 왼쪽이라, 점이 이미 안 보이는 틱에 제거되지 않고 1버킷 뒤에 제거됐다(지연은 틱 레이트와 무관한 상수 1버킷). SPEC·JSDoc 의 "(a)와 정확히 등가" 서술과도 어긋났다 | grace 틱 도입(지연이 더 커짐), **(b) 를 "값 있는 점(finite y)" 기준으로 바꿔 `y=null` 축 패딩을 비활성으로 취급**(→ 철회: JSDoc·본 표 2026-06-30 행이 "뭐라도 보낸 series 는 활성"을 규약으로 못 박았고, `y=null` 은 값 없음 마커이지 소멸 신호가 아니다. 만료가 필요한 target 에 패딩을 계속 보내는 것은 소비자 결함) |
| 2026-08-21 | drag-select 진입 타입에 수직 bar 추가 — 범위는 기존 `getSelectionRange`(graphMin..graphMax 선형 보간)를 그대로 쓰고 `horizontal: true` 는 제외 | 소비처가 필요한 건 x 구간 range 뿐이고, 가로 막대는 x축이 값 축이라 x 드래그가 범주·시간 범위를 뜻하지 않는다. categoryMode bar 의 인덱스 슬롯 배치(`cArea = xArea / totalCount`)와 선형 보간이 어긋나 최대 약 1 막대 폭 오차가 남지만, 실측 전에 공용 범위 계산을 분기하면 zoom(`zoom.getRangeInfo`) 경로까지 회귀 범위가 커진다 | `getSelectionRange` 에 bar 슬롯 역산 분기(공용 경로 회귀 범위 ↑), 소비처에서 슬롯 역산(라이브러리 배치 기하를 소비처가 복제), bar 를 계속 미지원 |
| 2026-08-21 | 누적(stacked) 막대를 drag-select 대상으로 확정 — 게이트·범위 계산 코드 변경 없이 테스트·SPEC·문서로만 고정 | 누적은 차트 타입이 아니라 `data.groups` 로 표현되고 진입 게이트는 `options.type`·`options.horizontal` 만 읽으므로 이미 통과한다. 코드로 표현되지 않은 지원 범위는 게이트에 `groups` 조건을 끼워 넣는 변경으로 조용히 깨질 수 있어, 테스트로 고정하고 누적 시 y range 가 스택 총합 공간이라는 사실을 문서에 남긴다 | 게이트에 누적 분기 명시 추가(동작 동일한 죽은 조건), `Bar.findItems` 를 함께 구현해 `data` 페이로드까지 채우기(요구사항 아님 — 소비 경로는 `range` 만 읽는다), 문서만 갱신하고 테스트 생략(회귀 감지 불가) |
| 2026-08-21 | `Bar.findItems` 추가 — 드래그 구간에 x 구간이 **걸치기만 한** 막대도 담고(부분 겹침 허용), 누적이든 아니든 Line 과 같은 포인트 객체를 그대로 반환 | 막대는 점이 아니라 폭을 가지므로 완전 포함을 요구하면 드래그로 반쯤 덮은 막대가 조용히 빠져 사용자 직관과 어긋난다. 반환값을 Line 과 같게 두면 누적 차트에서 `.y`(누적 합)·`.o`(자기 값)가 둘 다 실려 소비처가 필요한 쪽을 고를 수 있고, 이미 누적 line 이 같은 계약으로 동작한다. 부수 효과로 bar + `zoom.use` + time 축 드래그 줌이 살아난다 — 기존에는 `data` 가 비어 `getRangeInfo` 의 `data.length` 가드에 걸려 `dragZoom` 이 호출조차 되지 않았고, 밴드만 그려졌다 사라지는 조용한 무동작이었다(크래시가 아니다) | 완전 포함만 담기(HeatMap 선례 — 반쯤 덮은 막대 누락), `.o` 만 담아 누적 합을 숨기기(Line 과 계약이 갈리고 축 range 와도 어긋남), bar 미지원 유지(소비처가 range 만 쓸 수 있음) |
| 2026-08-21 | 부분 겹침 규칙을 zoom 위임 경로에서도 그대로 쓴다 — `dragZoom` 에 완전 포함 필터를 두지 않음 | 막대는 면적이라 "드래그가 닿은 범주"가 일관된 규칙이고, 두 규칙 모두 일반적인 드래그에서는 같은 구간으로 줌된다 — 차이는 양 끝에서 최대 1 인덱스다. 대가는 EvChartGroup 에 line 과 bar 를 함께 넣으면 같은 x 구간 드래그가 차트에 따라 ±1 인덱스 다르게 줌되는 것. 더 큰 비정합은 규칙 선택과 무관하게 남는다 — `dragZoom` 은 `zoomInfoData[0]`(매치된 첫 시리즈) 만 보고, 그룹 막대의 하위 막대는 슬롯 안에서 좌우로 치우쳐 있어 시리즈 표시 순서·범례 상태가 줌 창을 바꾼다. 이를 없애려면 아이템이 아니라 슬롯·라벨에서 인덱스 범위를 유도해야 하고 그건 `dragZoom` 재설계다 | zoom 경로만 완전 포함으로 좁히기(bar 와 line 의 규칙이 한 함수 안에서 갈리고, 위 시리즈 선택 비정합은 그대로), `findSelectedItems` 에 모드 인자 추가(공용 경로에 zoom 전용 분기 유입) |
| 2026-08-21 | 리스너 없이 `dragSelection.use` 만 켠 차트의 `dragEnd` 를 옵셔널 호출로 가드 (`this.options.zoom?.getRangeInfo?.(args)`) | `getRangeInfo` 는 zoom 모드에서만 주입되고 `DEFAULT_OPTIONS.zoom` 에는 없다. `@drag-select` 미바인딩 + zoom 미사용 조합이 mouseup 에서 TypeError 였다 — bar 가 새로 열려 이 조합에 도달하는 소비처가 늘어난다 | `else if (zoom?.use)` 로 분기 자체를 막기(`dragInfoBackup` 등 기존 부수효과 위치가 바뀜), 리스너 필수로 문서화(런타임 크래시를 문서로 막을 수 없음) |
| 2026-08-24 | `Bar.findItems` 의 탐색 대상을 `binarySearchBar` 와 같은 가시 윈도우 산술(`visibleStartIndex` ~ `filteredCount`)로 좁힘 | 좌표 존재 여부(`xp !== null`)만으로는 스크롤바 이동 후의 잔여 좌표가 걸러지지 않는다 — `computeGeometry` 는 윈도우만 순회하고, 스크롤 이동은 `lightUpdate` 라 `xp` 를 null 로 되돌리는 `createDataSet` 을 건너뛴다. 그래서 화면 밖 막대가 `data` 에 담겼다(실측: 윈도우를 `[0,4]`→`[5,9]` 로 옮긴 뒤 전체 폭 드래그가 10건 반환). 빈 윈도우 sentinel `(0, -1)` 도 같은 산술로 함께 막힌다. 같은 파일의 히트 테스트 두 경로가 이미 이 산술을 쓰고 있어 새 규약이 아니다 | `createDataSet` 을 항상 호출해 좌표를 리셋(스크롤 프레임마다 전체 데이터 재변환 — `lightUpdate` 의 존재 이유를 무력화), `computeGeometry` 에서 윈도우 밖 좌표를 null 로 지우기(윈도우 밖까지 순회해 부분 순회 이득 상실), 소비처에서 range 로 재필터(라이브러리 배치 기하를 소비처가 복제) |
| 2026-08-24 | 시리즈 `type` 오버라이드로 bar 를 섞은 line·scatter 차트의 `drag-select` 페이로드에 그 막대가 실리는 것을 의도된 계약으로 확정 — 코드 변경 없이 테스트·SPEC·API 문서로 고정 | `findSelectedItems` 는 `options.type` 을 읽지 않고 `seriesList` 전부를 훑으므로, `Bar.findItems` 가 생긴 순간 콤보 구성의 페이로드도 함께 바뀐다. 이전에는 그 막대가 조용히 빠져 있었으니 지금이 더 정확하다. 차트 타입으로 좁히려면 시리즈 메서드에 차트 타입을 넘기는 배선이 새로 생기고 페이로드는 오히려 부정확해진다 | `findSelectedItems` 에 차트 타입 게이트 추가(페이로드가 덜 정확해지고 공용 경로에 타입 분기 유입), `Bar.findItems` 에서 콤보를 판별해 빠지기(시리즈가 차트 타입을 알아야 함), 문서만 갱신하고 테스트 생략(게이트가 나중에 끼어들어도 감지 불가) |
| 2026-08-24 | drag-select 진입 게이트에 콤보(`options.type` falsy)를 추가 — 가로 조건은 bar 와 동일하게 `!horizontal` | 콤보는 `options.type` 을 두지 않고 시리즈가 각자 `type` 을 선언하므로 게이트의 타입 열거를 하나도 만족하지 못해 `dragSelection.use: true` 가 조용히 무시됐다. 하류는 이미 이 상태를 다룬다 — `dragStart`·`dragEnd` 의 분기는 `scatter`/`heatMap` 만 특수 처리하고 나머지는 line·bar 와 같은 y 밴드·`getSelectionRange` 경로이며, `findSelectedItems` 는 타입을 읽지 않는다. 즉 게이트 한 줄이 유일한 차단점이었다. falsy 판정이 넓어 `options.type` 없이 시리즈 레벨 `type` 으로만 선언한 pie·heatMap 도 통과하지만 둘 다 온전한 구성이 못 된다(heatMap 은 `data.labels` 가 `{ x, y }` 형태여야 하고, pie 는 축이 없어 `getSelectionRange` 가 `null` 이다 — 그 `null` 은 `d9c03415` 에서 zoom 위임 분기를 좁혀 가드했다). `dragSelection.use` 기본값이 false 라 영향은 명시적으로 켠 소비처에 한정된다 — 단 zoom 모드는 타입을 가리지 않고 `dragSelection.use` 를 덮으므로(`uses.js:582-587`) EvChartZoom 안의 콤보는 드래그 줌이 새로 동작한다. 콤보 예제의 x축이 `time` 이라 실제로 줌까지 간다 | `options.combo` 플래그로 판정(코드에서 읽히지 않는 죽은 기본값이다 — `SPEC.md` 기술이 부정확했고 이번에 정정), 시리즈 인스턴스를 훑어 `findItems` 보유 여부로 판정(게이트가 시리즈 생성 이후에 의존하게 되고 mousedown 마다 순회), 콤보 전용 옵션 신설(소비처가 이미 표현한 구성을 다시 선언해야 함) |
| 2026-08-26 | `dragEnd` 의 zoom 위임 분기를 `else if (args.range)` 로 가드 — 범위를 못 구한 구성은 위임하지 않는다 | 콤보 게이트가 `options.type` falsy 를 통과시키므로 축을 선언하지 않은 구성(시리즈 레벨 `type: 'pie'`)이 드래그를 시작할 수 있고, `axesSteps` 가 비면 `getSelectionRange` 가 `null` 이라 `args.range.dragSelectionInfo` 대입이 mouseup 에서 TypeError 였다 — 리스너 미바인딩·zoom 위임 두 경로 모두. 같은 else 분기의 인접 크래시(`getRangeInfo` 미주입)를 이미 옵셔널 호출로 막았으므로 함께 막는다. `step` 축 range 를 `NaN` 에서 `null` 로 바꾸는 후속 작업도 이 가드를 전제로 한다 | 게이트에서 축 없는 구성 제외(mousedown 판정이 축·시리즈 상태에 의존하게 되고, 그 시점에 축이 아직 없을 수 있다), `dragSelectionInfo` 대입만 가드하고 위임은 유지(`dragZoom` 이 `range` 를 구조분해하므로 안전이 `uses.js` 의 `zoomInfo.range` 검사에 걸린다), 문서로만 명시(런타임 크래시를 문서로 막을 수 없다) |
| 2026-09-04 | 드래그 중 hover 갱신을 열되, overlay clear→draw 소유권을 `dragMove` 한 곳에 두고 이 경로에는 `throttledMove` 를 적용하지 않음 | 드래그 중 툴팁이 시작 지점에 얼어붙어 종료 지점을 알 수 없었다(#2340). canvas mousemove 와 window `dragMove` 가 각자 clear/draw 하면 순서가 갈려 hover 또는 밴드가 깜빡이므로, 한 프레임 안에서 hover → 밴드 순으로 그리는 단일 소유자가 필요하다. throttle 을 걸면 clear 와 draw 가 프레임을 넘나들어 같은 깜빡임이 돌아온다 | `onMouseMove` 가드만 제거(양쪽이 서로의 그림을 지움), 드래그 중 밴드를 별도 캔버스로 분리(scatter 전용 `displayFromStartArea` 경로를 전 타입으로 확대 — 레이어 1장 추가 비용) |
| 2026-09-04 | 드래그 중 툴팁에 넘기는 `dragRange` 를 `getSelectionRange` **재호출**로 구하고(평행 계산 금지), 드래그 중에는 커스텀 툴팁 fast path 를 끔 | 라이브 헤더와 mouseup 페이로드가 같은 드래그에서 갈리면 안 된다. 라벨 스냅은 라벨 간격만큼, 같은 보간식을 따로 쓰는 것은 clamp·`Math.ceil`·블록 스냅 보정만큼 어긋난다. fast path 를 켜두면 같은 데이터 포인트 위에서 헤더가 얼어붙어 최대 라벨 간격만큼 뒤처진다 | 데이터 라벨 스냅(range 와 어긋남), 보간식을 툴팁 쪽에 복제(보정 누락으로 수 초 오차), 스냅 라벨과 보간값을 둘 다 전달(필드 4개로 계약 비대) |
| 2026-09-07 | 데이터 없는 지점의 드래그 구간 표시를 `dragSelection.showTooltipOnEmpty`(기본 false)로 옵션화하고 `tooltip.formatter.html` 경로만 지원 | 전 시리즈가 bar 면 라벨 스냅이 꺼져 막대 사각형만 히트하므로 막대 사이 간격에서 항상 무히트고, line 은 all-null 라벨이 스냅 후보에서 빠져 gap 중앙이 snapThreshold 밖이면 인덱스가 -1 이다. 어느 쪽이든 밴드와 `range` 는 갱신되는데 툴팁만 사라져 어느 구간을 잡고 있는지 알 수 없었다(영상 확인). 캔버스 툴팁까지 열면 구간 값(숫자·timestamp)을 무엇으로 표기할지 라이브러리가 정해야 해 축 포맷 재사용 결정이 새로 붙는 반면, `formatter.html` 소비처는 이미 그 포맷을 갖고 있다. 기본 false 라 기존 소비처 동작은 그대로다. 이렇게 띄운 툴팁은 `dragEnd` 에서 감춘다 — `keepDisplay: false`(원 요청 구성)면 mouseup 때 밴드까지 지워져 행 없는 헤더만 남는다 | 옵션 없이 항상 표시(데이터 없는 곳마다 빈 툴팁이 따라다녀 드래그 밖 hover 감각과 어긋남), 캔버스 툴팁까지 지원(구간 값 포맷 결정과 타이틀 전용 렌더가 새로 필요), hover 스냅 반경을 넓혀 무히트 자체를 없애기(드래그와 무관한 일반 hover 판정까지 바뀜) |
| 2026-09-07 | 드래그 중 재렌더가 비운 hover 를 `update()` 꼬리(`restoreDragArtifacts`)에서 `dragMove` 가 남긴 마지막 이벤트로 재생 — `updateTooltip` 의 tooltipDOM 비우기 자체는 건드리지 않음 | 재렌더는 `clear()` 로 overlay 를 항상 비우고, `updateTooltip` 이면 tooltipDOM 까지 비운다. 커서가 멈춰 있으면 되살릴 mousemove 가 오지 않아 드래그 중 아티팩트가 다음 커서 이동까지 사라져 있었다 — 실측으로 두 층이 갈린다. 데이터만 갱신하면 툴팁 DOM 은 남지만 하이라이트·인디케이터가 지워지고 내용이 직전 프레임에 고정된다(마우스 정지 상태에서 값이 5틱 내내 동일). options 객체를 매 틱 새로 만드는 소비처(대시보드 위젯의 정상적인 Vue 패턴)는 `formatter.html` 함수 identity 가 달라져 `Chart.vue` 의 `isEqual(newOpt.tooltip, ...)` 가 항상 false → `updateTooltip: true` → `innerHTML = ''` 로 툴팁이 통째로 사라진다. `update()` 는 이미 같은 자리에서 밴드를 다시 그리고 있었으므로 hover 도 같은 자리에서 같은 순서로 복원하면 프레임 구성이 `dragMove` 와 동일해진다. 비우기를 드래그 중 건너뛰면 툴팁 레이아웃 초기화가 밀려 옵션 교체가 반영되지 않는다 | 드래그 중 `innerHTML` 비우기를 스킵(옵션 교체가 반영되지 않고 stale 마크업이 남음), 마지막 hitInfo 를 캐시해 그리기(좌표가 재렌더로 바뀌어 hit 을 다시 구해야 함 — 이벤트를 남기는 편이 재계산 지점이 하나), 소비처에 options 객체 재사용을 문서로 요구(라이브러리가 막을 수 없고 정상적인 Vue 패턴이다) |
| 2026-09-07 | 커스텀 툴팁 비가상 경로의 `formatter.html` 예외를 호출 지점에서 가두고 툴팁만 포기(경고 인스턴스당 1회) | 빈 `seriesList` 호출은 `itemsCount` 가 0이라 가상 경로 게이트를 통과할 수 없어 항상 비가상 경로인데 그쪽에는 보호가 없었다. `showTooltipOnEmpty` 로 첫 항목을 무조건 참조하던 기존 formatter 가 던지면, 호출부가 `overlayClear` 를 먼저 하므로 그 프레임의 하이라이트와 드래그 밴드까지 사라지고 `restoreDragArtifacts` 경로에서는 `update()` 밖으로 나가 재렌더 예약 플래그가 정리되지 않는다. 가상 경로가 이미 같은 호출을 try/catch 로 감싸 fallback 신호로 쓰고 있어 선례가 있다. 드래그 중에는 프레임마다 불려 경고가 폭주하므로 `_vsWarnedFallback` 과 같은 1회 플래그를 둔다 | `drawHoverArtifacts` 를 통째로 감싸기(예외 지점이 흐려지고 밴드 순서 보장이 어려움), 드래그 경로만 감싸기(일반 hover 의 같은 예외는 그대로 남음), 예외를 올려 소비처가 알게 하기(라이브러리 상태가 프레임 단위로 깨진다) |
| 2026-09-07 | 드래그 경로의 툴팁 hide 는 `tooltip.debouncedHide` 를 우회한 즉시 hide(`hideTooltip`) — 문서의 "드래그가 끝나면 즉시 사라진다" 를 코드로 맞춤 | `hideTooltipDOM` 은 `debouncedHide: true` 면 200ms trailing 디바운스다. 드래그는 프레임마다 hide 를 부르므로 커서가 캔버스 밖에서 움직이는 동안 타이머가 계속 리셋돼 **툴팁이 끝까지 감춰지지 않는다** — 밴드는 clamp 좌표로 갱신되므로 서로 다른 두 구간이 동시에 보인다. 디바운스의 목적은 짧은 이탈·재진입의 깜빡임 억제인데 드래그 경로에는 그 전제가 없다. hit 이 있는 `returnValue` 경로(`plugins.interaction.js:92`)는 라이브러리가 `tooltipDOM` 을 그리지 않아 보일 툴팁이 없으므로 그대로 둔다. 기본값이 `false` 라 대다수 소비처에서는 동작이 동일하다 | 문서에 `debouncedHide` 조건을 덧붙이기(md·json 9곳에 예외 문장이 붙고 실제 증상은 남음), 드래그 시작 때 디바운스를 취소(대기 타이머만 없애 프레임마다 리셋되는 문제는 그대로), `hideTooltipDOM` 자체를 즉시로 되돌리기(옵션 계약 파괴) |
| 2026-09-07 | 드래그 중 커스텀 툴팁 가상 스크롤의 프레임당 재초기화(teardown→reinit)를 이 변경에서 병합하지 않고 비용만 기록 | 드래그 경로는 `tooltip.throttledMove`(기본 true)를 타지 않고 fast path 도 꺼지므로, 시리즈가 임계(기본 50)를 넘으면 raw mousemove 마다 `drawCustomTooltipVirtual` 이 세션을 다시 만든다. 변경 전에는 드래그 중 hover 파이프라인이 전면 차단돼 이 비용이 0이었다. 헤더가 커서 픽셀마다 달라져 formatter 재실행 자체는 건너뛸 수 없고, 남은 레버는 hover→밴드를 rAF 콜백 하나로 묶는 것인데 그 순서가 이 기능의 유일한 깜빡임 방어선이라(2026-09-04 결정) 같은 변경에서 함께 바꾸면 회귀 원인을 가른다. 대상은 대규모 시리즈 + 커스텀 formatter + dragSelection 이 동시에 걸린 구성으로 좁다 | rAF 병합을 지금 도입(밴드/hover 순서 회귀 위험을 이 변경과 섞음), `throttledMove` 를 드래그에도 적용(clear 와 draw 가 프레임을 넘나들어 깜빡임 복귀), 드래그 중 가상 스크롤 비활성(같은 프레임에서 마크업 구조가 달라져 스크롤 위치·학습 평균이 초기화됨) |
| 2026-09-07 | 드래그 중 커스텀 툴팁 가상 스크롤의 프레임당 재초기화(teardown→reinit)를 그대로 둠 | 드래그 경로는 `tooltip.throttledMove`(기본 true)를 타지 않고 fast path 도 꺼져 raw mousemove 마다 `drawCustomTooltipVirtual` 이 세션을 다시 만든다. 실측(고비용 formatter + 시리즈 N개, 드래그 프레임당 mean)에서 지분이 갈렸다 — hover 총비용은 N=100 4.13ms · N=200 7.98 · N=400 14.13 인데 그중 소비처 `formatter.html` 이 60~70%(2.27 / 4.61 / 8.96)이고 라이브러리 몫은 1.50 / 2.55 / 3.81 이다. 가상 경로를 끄면 오히려 나빠진다 — N=100 에서 hover 가 10.07ms(2.4배)로, 100행 전체가 DOM 에 붙어 `setCustomTooltipLayoutPosition` 의 `offsetWidth`/`offsetHeight` 가 강제 레이아웃을 일으킨다(약 6.5ms). 재초기화는 그보다 비싼 레이아웃을 대체하는 비용이다. rAF 병합은 병합할 중복이 없다 — mousemove 는 프레임당 최대 1건이고(201프레임/201건) 핸들러가 느려지면 더 적게 온다(548프레임/121건). 체감 문제가 생기는 구간은 시리즈 수백 개(headed N=400 hover 54.74ms, 33ms 초과 82/582 프레임)인데 그중 59%가 formatter 라 재초기화를 없애도 38.9ms 로 2프레임을 넘는다 — 제안 방향으로는 유일한 문제 구간이 해결되지 않는다 | rAF 병합(이벤트가 이미 프레임당 1건 이하라 이득 없음), `throttledMove` 를 드래그에도 적용(overlay clear 와 draw 가 프레임을 넘나들어 깜빡임 복귀), 드래그 중 가상 스크롤 비활성(실측 2.4배 악화), 드래그 중 커스텀 툴팁 DOM 재그리기만 throttle(overlay 가 아닌 별도 DOM 이라 깜빡임 사유가 적용되지 않고 N=400 비용을 약 1/3 로 깎지만, 헤더가 최대 30ms 지연되고 문제 구간인 수백 시리즈 구성이 아직 관측되지 않았다 — 리포트가 들어오면 이쪽) |
| 2026-09-07 | 가상 경로 `formatter.html` 예외의 콘솔 경고만 인스턴스당 1회로 묶고, 재시도 차단(`_vsDetectFailed`)은 하지 않음 — 프레임당 formatter 2회는 남긴다 | 예외 분기는 `857803d1` 이 둔 단락의 대상이 아니었다. row 탐지 실패는 `_shouldVirtualizeCustomTooltip` 게이트에서 막혀 다음 mousemove 부터 formatter 1회인데, 예외는 `return false` 로 비가상 경로에 넘겨 그쪽이 formatter 를 한 번 더 부른다. 드래그 경로는 raw mousemove 라 경고가 프레임 수만큼 쌓인다 — 경고는 설정 오류 신호라 1회로 충분하다. 2회 호출까지 없애려면 예외에도 `_vsDetectFailed` 를 세워야 하는데, 특정 데이터 포인트에서만 던지는 formatter 는 플래그가 `render()` 에서만 리셋되므로 그 드래그 내내 전체 부착 경로에 고정된다 — 실측으로 N=100 hover 4.13ms → 10.07ms(2.4배)다. 던지는 프레임은 어느 경로로도 툴팁이 없으니 남는 비용은 깨진 구성에서만 발생하는 소비처 호출 1회다 | 예외에도 `_vsDetectFailed` 세우기(일시적 예외가 드래그 전체를 2.4배 경로로 고정), 가상 catch 에서 툴팁을 감추고 `return true`(비가상 경로가 먼저 부르는 `_teardownCustomTooltipVirtualScroll` 이 건너뛰어져 scroll 리스너·ResizeObserver 가 붙은 세션이 감춰진 툴팁에 남는다), 경고 제거(설정 오류를 알 방법이 없어진다) |
| 2026-09-14 | 툴팁 배치 한계를 `document.body.clientWidth/Height` → 가시 영역(`scrollX/Y + documentElement.clientWidth/Height`)으로 바꾸고, 배치 지점부터 가시 영역 우단까지 남은 폭을 `max-width` 상한으로 건다(+ `.ev-chart-tooltip` `box-sizing: border-box`). 두 배치 경로를 `placeTooltipDOM` 하나로 합침 | 커스텀 툴팁이 문서 가로 스크롤바를 깜빡이게 했다. 원인은 둘 — ① body 가 뷰포트보다 넓은 레이아웃에서 `body.clientWidth` 한계가 화면 밖이라 반전이 일어나지 않았고, ② 배치 이후 폭이 커지는 경로(가상 스크롤 rAF 렌더·내부 스크롤바 출현·웹폰트·라이브 갱신)가 있어 배치 시점 계산만으로는 못 막았다. 폭 상한은 레이아웃 단계에서 막으므로 넘친 프레임 자체가 생기지 않는다 — 사후 교정은 한 프레임 늦어 그 프레임이 그대로 그려진다(실측: `overNextFrame: 0` 인데 육안 깜빡임 지속). `border-box` 는 인라인 `border: 1px` 를 상한 안에 넣어 호스트 앱 전역 리셋 유무로 기준이 흔들리지 않게 한다(미지정 시 2px 누수 실측). 세로는 상한을 걸지 않았다 — 재현된 깜빡임이 가로였고, 높이는 `tooltip.maxHeight`·`overflow-y: hidden` 으로 이미 제한돼 증가 폭이 작다 | ResizeObserver 재클램프(한 프레임 지연으로 깜빡임 잔존 + hover 당 콜백 비용 — 적용 후 철회), 내용 갱신 전 위치 선반영(같은 태스크라 페인트되지 않는 상태만 막아 무효 — 철회), 반전 판정 폭을 tooltipDOM 실측으로 교체(당시엔 폭 상한이 넘침을 이미 막아 불필요로 보고 철회 → **2026-09-15 에 채택**: 상한은 tooltipDOM 에 걸리는데 판정은 콘텐츠 폭이라 기준이 어긋나면 반전 없이 상한만 걸려 내용이 잘린다 — 아래 행 참조), `position: fixed` + clientX/clientY 전환(구조적 해결이나 `.ev-chart-tooltip` position 을 덮어쓴 외부 소비자 CSS 를 깨뜨릴 수 있어 3.x 보류) |
| 2026-09-15 | 툴팁 가로 반전 방향에 히스테리시스 도입 — 직전 방향을 유지하고 그 방향으로 두면 가시 영역을 벗어날 때만 전환한다(양쪽 다 불가한 경우는 아래 행에서 좌단 고정으로 교체). `hideTooltipDOM` 에서 방향 기억을 초기화 | 반전을 매 호출의 폭으로만 판정하면, hover 지점마다 폭이 달라지는 차트에서 커서가 한 방향으로 움직이는 동안에도 좌우가 번갈아 뒤집힌다 — 실측(chromium, 차트를 가로질러 63지점 스윕)에서 폭이 309→533→253 으로 변동하며 반전이 **7회** 전환됐다. 이전 코드에서는 `body.clientWidth` 기준이라 반전 자체가 거의 일어나지 않아 가려져 있었고, 배치 한계를 가시 영역으로 고치면서 표면화됐다. 양쪽 다 안 들어갈 때 오른쪽을 택하는 것은 왼쪽 배치가 내용 앞부분(시리즈 이름)을 잘라내기 때문 | 커서가 가시 영역 오른쪽 절반이면 무조건 반전(폭 무관이라 요동은 없으나 작은 툴팁도 왼쪽에 떠 기존 동작이 크게 바뀜), 세션 최대 폭을 기억해 판정(폭이 한 번 커지면 이후 계속 왼쪽에 붙어 커서와 멀어짐), 첫 배치 폭 측정 시점 교정(→ 한 지점에 머무는 실측에서는 배치 시점 폭과 안정화 폭이 동일해 원인이 아니라고 봤으나, 커서 이동 중에는 실제로 폭이 커진다 — 아래 2026-09-15 우단 고정 행 참조) |
| 2026-09-15 | 반전 배치를 좌표 대신 `translateX(-100%)` 로 **우단 고정**(앵커 = `커서 - 20px`)하고, 폭 상한도 앵커 기준(앵커→가시 영역 좌단)으로 전환 | 반전 배치의 좌측을 `커서 - 폭 - 20` 으로 잡으면 배치 이후 폭이 커지는 경로(가상 스크롤 재렌더·ResizeObserver 재측정)에서 툴팁이 **커서 쪽으로 자라 커서를 덮는다**. 실사용 콘솔 실측에서 `posX` 가 고정인 채 폭만 커지는 프레임이 반복 관측됐다(배치 폭 404 → 직후 471, +65px 패턴). 우단을 고정하면 폭 증가가 왼쪽으로만 향해 커서를 넘지 않는다. 브라우저 회귀로 검증 — 좌측 고정으로 되돌리면 겹침 34프레임. 같은 날 도입한 방향 히스테리시스와는 **다른 문제**다(제거 실험 시 반전 전환 3·7회 재발), 둘 다 유지한다 | 배치 후 ResizeObserver 로 재배치(한 프레임 늦어 덮인 프레임이 그대로 그려진다), 툴팁 폭을 고정값으로 묶기(내용이 잘린다) |
| 2026-09-15 | 툴팁이 가시 영역보다 넓어 **양쪽 다 안 들어가는 경우**는 히스테리시스 대신 **남은 폭이 넓은 쪽**으로 보낸다. 반전 판정 폭을 콘텐츠 폭 → **tooltipDOM 실측 폭**으로 교체. 방향 기억 초기화를 `resetTooltipPlacement` 로 모아 `tooltipClear` 에도 걸고, `placeTooltipDOM` 에서 예약된 `debouncedHide` 를 취소 | 리뷰에서 나온 세 결함이 모두 "판정 기준과 실제 대상이 어긋난다"는 같은 뿌리였다. ① 양쪽 다 불가일 때 커서 옆 20px 를 고집하면 커서가 우단 20px 안에 있을 때 남은 폭이 음수 → `max-width: 0` → 테두리만 남고 내용이 통째로 사라진다. 기존 "오른쪽을 택한다"의 근거(왼쪽은 앞부분이 잘린다)도 우단 고정 전환 이후엔 성립하지 않는다(어느 쪽이든 상한에 눌려 잘린다). **가시 영역 좌단 고정은 적용 후 철회** — 브라우저 회귀(800×600, 폭 253~533 스윕)에서 이 구간을 드나들 때마다 툴팁이 화면 끝으로 점프해 반전 전환이 1→3회로 늘었다. 남은 폭이 넓은 쪽은 커서 위치의 단조 함수라 한 방향 스윕에서 전환이 최대 1회다. ② 상한은 `box-sizing: border-box` 인 tooltipDOM 에 걸리는데 판정 폭은 자식 콘텐츠 폭이라, 커스텀 경로는 인라인 `border: 1px` 로 2px, 캔버스 경로는 header padding(호스트 전역 리셋이 없으면 content-box) + 테두리로 최대 34px 어긋난다. 그 구간에 커서가 있으면 반전 없이 상한만 걸려 값 칸부터 잘린다 — docs 앱은 `* { box-sizing: border-box }` 덕에 가려져 있었다. ③ mouse-leave 의 주 경로는 `hideTooltipDOM` 이 아니라 `tooltipClear` 라 방향 기억이 남았고, `debouncedHide` 의 트레일링 콜백은 취소되는 곳이 없어 재표시된 툴팁을 숨기며 방향까지 지웠다 | `Math.max(0, room)` 유지(0px 툴팁을 증상으로만 가린다), 방향 초기화를 debounce 바깥 호출부로 이동(툴팁이 아직 보이는데 방향만 지워져 재히트 시 반대편으로 튄다 — 취소가 맞는 해법), `.ev-chart-tooltip-header/-body` 에도 `box-sizing: border-box` 하달(호스트별 헤더 폭 편차까지 없애지만 리셋 없는 소비자에서 헤더가 32px 좁아져 텍스트가 잘린다 — 이번 범위 밖) |
| 2026-09-15 | 세로 배치에 **가시 영역 상단 하한**(`Math.max(view.top, …)`)과, 위·아래 모두 안 들어갈 때 **남은 공간이 넓은 쪽을 택하는 규칙**을 추가 | 반전은 하는데 높이 상한이 없어(2026-09-14 행) 위 공간보다 높은 툴팁은 반전 좌표 `커서 y - 높이 - 20` 이 음수가 되고 상단이 화면 밖으로 잘렸다 — 스크롤로 되돌릴 수 없는 손실이라 가로의 우측 넘침보다 나쁘다. 이 결함 자체는 `document.body.clientHeight` 를 쓰던 시절부터 있었으나, 한계를 가시 영역으로 좁히면서 위 반전이 훨씬 자주 일어나 노출이 커졌다. 하한만으로 충분한 이유는 아래 방향은 문서 좌표라 넘쳐도 스크롤로 닿기 때문이다. 양쪽 다 안 들어갈 때 넓은 쪽을 택하는 것은 가로와 같은 규칙으로, 아래가 넓은데도 위로 반전해 커서·차트를 덮는 배치를 막는다 | 가로처럼 남은 높이를 `max-height` 상한으로 거는 대칭 해법(툴팁 본문 높이를 `tooltipBodyDOM.style.height` 로 실측 고정하고 가상 스크롤 행 높이를 그 위에서 재는 구조라, 상한이 측정을 눌러 스크롤 계산이 무너질 위험이 있어 후속으로 분리), `tooltip.maxHeight` 를 가시 영역 높이로 자동 클램프(소비자가 지정한 옵션 값을 라이브러리가 조용히 바꾸는 것이라 철회) |
| 2026-09-17 | 폭 상한을 **상한이 실제로 구속되는 구간(툴팁이 커서 양옆 어디에도 안 들어감)에서만** 16px 단위로 내림한다(`quantizeTooltipMaxWidth`) | 상한은 커서 좌표의 함수라 1px 이동마다 값이 달라지는데, 구속되는 구간에서는 그 값이 그대로 툴팁의 used width 가 된다. 가상 스크롤의 폭 감시 ResizeObserver 가 mousemove(30ms throttle)마다 발화해 `measured`/`heights` 전량 무효화 + prefix sum 재계산 + 앵커 `scrollTop` 보정을 돌렸다 — 같은 데이터 포인트 위에서는 `skipCustomTooltipRedraw` 로 redraw 를 건너뛰는데도 이 경로만 계속 돌아, 커서만 움직여도 툴팁 내부 스크롤이 흔들린다. 내림이라 항상 남은 폭 이하여서 넘침 차단 계약은 유지된다 | 값 캐싱 후 변경 시에만 쓰기(측정 보장을 위해 `maxWidth = ''` 를 매 호출 무조건 지우므로 — `setCustomTooltipLayoutPosition`·`drawCustomTooltip` — 같은 버킷이면 쓰기를 건너뛰어 상한이 통째로 사라진다), 구간 구분 없이 일괄 양자화(`fits` 구간의 상한이 실측 폭 아래로 내려가 멀쩡히 들어가던 내용이 줄바꿈되고, 상한이 구속되면서 없던 RO 발화가 새로 생긴다 — SPEC "상한은 항상 실측 폭 이상" 계약 위반) |
