# Scheduler (EvScheduler) — Decisions

<!-- 날짜 `-` = SPEC 아키텍처 관찰 기반 초기 결정. -->

| 날짜 | 결정 | 이유 | 대안 |
|------|------|------|------|
| - | 선택 결과를 셀 좌표 배열(modelValue)로 표현 | 격자 선택을 직렬화 가능한 단순 구조로 유지 | 2D 불리언 매트릭스(희소 선택에 비효율) |
| - | 행/열 라벨 배열로 격자 차원 결정 | 요일×시간 등 임의 축을 라벨만으로 구성 | 고정 격자(재사용성 저하) |
