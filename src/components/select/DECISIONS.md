# Select (EvSelect) — Decisions

<!-- 날짜 `-` = SPEC 아키텍처 관찰 기반 초기 결정. -->

| 날짜 | 결정 | 이유 | 대안 |
|------|------|------|------|
| - | modelValue 타입을 boolean/string/number/array/object 로 폭넓게 허용 | 단일/다중/객체 값 선택을 한 컴포넌트로 수용 | 타입 고정(사용처 제약) |
| - | 드롭박스 위치를 뷰포트 기준 동적 계산 | 화면 하단 근처에서 위로 열려 잘림 방지 | 항상 아래로 열기(잘림) |
| - | 다중 선택에 태그 접기(collapseTags) 옵션 | 선택이 많을 때 입력 영역 비대화 방지 | 전체 태그 노출(레이아웃 붕괴) |
| 2023-05-24 (소급) | 필터에 초성 검색 + 한/영 자판 변환 매칭 도입 (`korean-regexp`) | 한글 자음만 입력하거나 한/영 변환 없이 입력해도 검색되는 편의 기능 (PR #1405 / issue #1404) | 단순 `includes` 부분일치(변환 없이 입력 시 검색 실패) |
| 2026-08-14 | 검색어 강조 설정을 단일 `highlight` 객체 prop 으로 제공 | `match`·`color` 는 목적이 같아 함께 켜고 끄는 값 — prop 표면을 하나로 유지 | `highlightMatch`/`highlightColor` 개별 prop(연관성 흐려지고 표면 증가) |
| 2026-08-14 | 위 편의 검색을 끄는 opt-out prop 을 추가하지 않음 | 의도된 편의 기능이며, `sk` 로 `나` 가 검색되고 강조되는 것이 올바른 동작으로 확인됨 | `smartFilter` 등 Boolean prop 추가(불필요한 API 표면 증가) |
| 2026-08-14 | 강조용 global 매처를 `computed`(`globalMatchers`)로 유지 | `splitByMatch` 는 항목마다 호출되므로 함수 안에서 만들면 항목 300개 기준 키 입력 1회당 정규식 900개가 컴파일된다. computed 로 올려 검색어당 3개로 줄었고, lazy 라 강조 off 사용처에는 컴파일이 발생하지 않는다(키 입력 1회당 강조 off 3개 / on 6개 = 필터용 3 + 강조용 3) | 항목마다 재컴파일(타이핑 지연) |
| 2026-08-14 | 강조 조각을 `itemChunks` computed 로 미리 계산 (템플릿에서 `splitByMatch` 직접 호출하지 않음) | 템플릿 호출은 검색어가 그대로여도 리스트가 리렌더될 때마다 전 항목을 다시 분해한다. `filteredItems` 와 인덱스가 1:1이라 computed 로 올려도 렌더 구조는 그대로 | 템플릿에서 `splitByMatch(item.name)` 호출(항목 수가 커지는 사용처에서 리렌더마다 재분해) |
| 2026-08-14 | `highlight` validator 에서 알 수 없는 키도 거부 | `{ mach: true }` 같은 오타가 조용히 무시되면 "왜 강조가 안 되지"로 이어짐 — 개발 중 즉시 드러나게 함 | 관대한 통과(오타 디버깅 비용) |
| 2026-08-14 | 강조 구간을 필터와 동일한 매처(`getMatchers`)로 계산 | 필터에 걸린 이유가 그대로 강조되어야 사용자가 매칭 근거를 납득 — 별도 매처면 "걸렸는데 강조 없음" 불일치 발생 | 단순 `indexOf` 부분일치 강조(초성·자판 변환 검색과 어긋남) |
| 2026-08-14 | 매칭된 **모든** 구간 강조 (겹침은 병합) | 항목명에 검색어가 반복 등장할 때 일부만 강조되면 오히려 혼란 | 첫 매칭 1곳만 강조(반복 등장 시 누락) |
| 2026-08-14 | 강조 비활성 시 기존 단일 텍스트 렌더 경로를 그대로 유지 (`v-if="isHighlightMatch"` 분기) | 미지정 사용처의 DOM 구조를 바꾸지 않아 하위 호환 100% 보장 | 항상 chunk span 렌더(기존 DOM·CSS 셀렉터 깨질 위험) |
| 2026-08-14 | 기본 강조색을 테마 `primary` 로 두고 `color` 지정 시 inline style 로 덮어씀 | 라이트/다크 테마 전환에 자동 대응하며 컴포넌트 스타일 규약과 일관 | 고정 색상 하드코딩(테마 전환 시 대비 저하) |
| 2026-08-14 | 강조 범위를 드롭박스 항목명으로 한정 (선택 태그 제외) | 태그는 검색과 무관하게 상시 노출되어 강조 의미가 없음 | 태그까지 강조(검색 종료 후에도 잔상처럼 남음) |
| 2026-08-14 | 검색 input 에 IME composition 가드를 두지 않음 (조합 중에도 매 `input` 갱신) | 조합 중 갱신을 보류하면 입력창에는 조합 중 글자가 보이는데 목록/강조는 직전 검색어에 멈춰 둘이 어긋난다. Vue 는 바인딩 값이 DOM 값과 같으면 `:value` 를 다시 쓰지 않아 조합 버퍼도 끊기지 않음(검증 완료) | `compositionstart`/`end` 가드(표시값과 필터값이 조합 내내 불일치) |
