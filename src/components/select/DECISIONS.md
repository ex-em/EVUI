# Select (EvSelect) — Decisions

<!-- 날짜 `-` = SPEC 아키텍처 관찰 기반 초기 결정. -->

| 날짜 | 결정 | 이유 | 대안 |
|------|------|------|------|
| - | modelValue 타입을 boolean/string/number/array/object 로 폭넓게 허용 | 단일/다중/객체 값 선택을 한 컴포넌트로 수용 | 타입 고정(사용처 제약) |
| - | 드롭박스 위치를 뷰포트 기준 동적 계산 | 화면 하단 근처에서 위로 열려 잘림 방지 | 항상 아래로 열기(잘림) |
| - | 다중 선택에 태그 접기(collapseTags) 옵션 | 선택이 많을 때 입력 영역 비대화 방지 | 전체 태그 노출(레이아웃 붕괴) |
