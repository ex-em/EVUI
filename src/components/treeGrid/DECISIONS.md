# TreeGrid (EvTreeGrid) — Decisions

<!-- 날짜 `-` = SPEC 아키텍처 관찰 기반 초기 결정. YYYY-MM-DD = git 이력 근거 확인. -->

| 날짜 | 결정 | 이유 | 대안 |
|------|------|------|------|
| - | 이벤트 로직(uses.js)은 grid 와 별개로 독립 구현, UI 하위 컴포넌트만 grid 재사용 | 트리 계층(부모-자식 체크 전파, 조상/자손 필터, 노드 확장)이 평면 grid 와 근본적으로 달라 로직 공유가 어려움. 반면 페이지네이션/요약/컬럼설정 UI 는 동일해 재사용 이득 | grid 로직 전면 공유(트리 특수성 수용 불가), 전부 독립(UI 중복) |
| - | 부모-자식 체크 연동에 'each' 모드 분기 도입 | 트리에서 부모 체크의 자손 전파가 기본이지만, 노드 독립 체크가 필요한 사용처를 위해 opt | 항상 연동(독립 체크 불가) |
| - | 필터 시 매칭 노드의 조상/자손을 함께 표시 | 계층 맥락 없이 매칭 노드만 보이면 트리 구조가 깨져 이해 불가 | 매칭 노드만 표시(맥락 상실) |
| 2026-03-24 | vue-resize-observer 를 커스텀 `@/directives/resize` 로 교체 (grid 와 동시) | 외부 의존 제거·리사이즈 감지 일원화 | vue-resize-observer 유지 |
