# {도메인명}

## Purpose

<!-- 이 도메인이 해결하는 문제, 존재 이유. 예: "사용자 검색 필터의 상태 관리와 URL 동기화를 담당" -->


## Features

<!-- 이 도메인이 제공하는 기능들. 예: "- **다중 선택**: Ctrl/Shift 조합으로 여러 항목을 동시에 선택" -->

-


## Business Rules

<!-- 도메인 규칙·제약사항(성능·보안·접근성·가용성 등 비기능 요구사항 포함). 예: "- 필터가 비어 있으면 전체 목록을 반환한다" / "- 검색 응답은 p95 300ms 이하" -->

-


## Acceptance Criteria

<!-- 완료 여부를 판정하는 관찰 가능한 조건. 각 항목은 테스트(*.spec.js)·수동 QA(docs 예제) 중 하나로 확인 가능해야 한다. 해당 없으면 "해당 없음". 예: "- disabled 상태의 버튼은 클릭해도 click 이벤트를 emit 하지 않는다" -->

-


## Architecture

<!-- 컴포넌트 구성도, 설계 구조. 간단한 도메인은 한 줄 설명으로 대체 가능 -->

```
┌─────────────────────────────┐
│       MainComponent         │
├─────────────────────────────┤
│  ┌───────┐  ┌───────────┐  │
│  │ Sub A │  │   Sub B   │  │
│  └───────┘  └───────────┘  │
└─────────────────────────────┘
```


## File Structure

<!-- 파일별 역할. 예: "| FilterPanel | 필터 UI 컨테이너 |" -->

| 파일 | 역할 |
|------|------|
|      |      |


## Dependencies

<!-- 이 도메인이 의존하는 다른 도메인/외부 시스템. 예: "| 검색 API | 결과 조회 |" -->

| 대상 | 용도 |
|------|------|
|      |      |


## Glossary

<!-- 도메인 용어 정의. 해당 없으면 "해당 없음" 가능. 예: "| facet | 필터 가능한 속성 축 |" -->

| 용어 | 정의 |
|------|------|
|      |      |


## Data Flow

<!-- 데이터/상태 흐름. 간단한 도메인은 한 줄 흐름도도 가능. 예: "입력 → 검증 → 상태 업데이트 → API 요청 → 결과 렌더" -->

```
[이벤트/입력]
    │
    ▼
[처리 로직]
    │
    ▼
[상태 업데이트] → [emit/콜백] → [부모/외부 반응]
```
